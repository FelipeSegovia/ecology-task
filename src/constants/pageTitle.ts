export const APP_NAME: string = 'Liftun';

export const PAGE = {
  HOME: 'inicio',
  ACTIONS: 'accion',
};

const PAGE_TITLE = {
  HOME: `${APP_NAME} - ${PAGE.HOME}`,
  ACTIONS: `${APP_NAME} - ${PAGE.ACTIONS}`,
};

export default PAGE_TITLE;
